pip install -U scratchattach
import scratchattach as scratch3
import time
session = scratch3.login("a-free-follower", "001542016")
while 1==1:
  user.follow(user.follower_names(limit=1, offset=0))
  time.sleep(3) # Sleep for 3 seconds
